services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    image: price-scrapper:latest
    pull_policy: never
    ports:
      - "50051:50051"
    env_file:
      - .env
    environment:
      DB_ADDRESS: db
      DB_PORT: 5432
      MIGRATIONS: file:///app/db/migrations
    depends_on:
      db:
        condition: service_healthy

  gateway:
    image: price-scrapper-gateway:latest
    pull_policy: never
    build:
      context: .
      dockerfile: gateway/Dockerfile
    ports:
      - "8080:8080"
    depends_on:
      - app

volumes:
  pgdata:
